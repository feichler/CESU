{% for message in success %}
    <div class="system-message system-message-success system-message-dismissable">
        <button type="button" class="close" data-dismiss="alert">
            <span aria-hidden="true">
                &times;
            </span>
            <span class="sr-only">
                Close
            </span>
        </button>
        {{ message|msg }}
    </div>
{% endfor %}